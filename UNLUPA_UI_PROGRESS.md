# UNLUPA UI Progress & Recovery Documentation

## Project Context
- **Goal:** Redesign visual UI/UX UNLUPA secara nyata dengan standar kualitas inspirasi **Linear** dan **Raycast**.
- **Karakter Visual Target:** Modern, premium, clean, minimal, sangat rapi, strong visual hierarchy, intentional spacing, subtle borders, subtle elevation, smooth interaction, fully theme-aware (Light/Dark/System).
- **Pendekatan:** Preservasi penuh business logic, backend/API integration, authentication flow, dan routing. Tidak ada penghapusan fitur atau logic secara acak.

## Handoff Sesi Berikutnya (BACA DULU — Rule 12 & Rule 10)

> Status: **COMMIT 0d70d2d SELESAI** ("feat(ui): redesign visual tahap A (G0–G5) + bug hunt dashboard" — 170 file, `tsconfig.app.tsbuildinfo` sengaja dikecualikan). **B1 Typography landing SELESAI (item 17)** — belum diverifikasi browser. Berikutnya: **verifikasi browser B1** → pilih batch B2–B5.

- **Commit gabungan selesai** atas perintah eksplisit user (keputusan: "1 commit gabungan"). `tsconfig.app.tsbuildinfo` TETAP un-committed (build artifact) dan masih menunjukkan modified di working tree — jangan di-commit.
- **B1 SELESAI (item 17)** — tipografi landing (acuan linear.md/raycast.md; belum diverifikasi browser). Ringkasan di item 17.
- **Tahap A + G5** tetap LULOS/SELESAI (item 14-16). Semua pekerjaan berikutnya (item 17) di-commit menyusul setelah verifikasi browser batch.
- **Tahap B scope yang belum dikerjakan:** B2 Navbar+Footer, B3 Button/Input/Badge, B4 Card & surface ladder, B5 font-cinzel non-landing (Sidebar.tsx & ComingSoonRoomPage masih pakai `.font-cinzel` yang hanya didefinisikan di `alquran.css`, diimpor hanya AlquranPage → tidak teraplikasi).
- **File pilar wajib dibaca:** `UNLUPA_GOLDEN_RULES.md`, `UNLUPA_UI_PROGRESS.md`, `docs/design/references/linear.md`, `docs/design/references/raycast.md`, `UNLUPA_AGENT_EXECUTE_TODAY.md`. Prinsip: restraint, 1 aksen teal, hairline border, surface ladder, tanpa glow/rainbow/glass, copy Indonesia jangan diganti.
- **File paling baru diubah:** 9 section landing (About, Akad, Feature, Metodologi, Solution, Testimonial, Inklusivitas, FAQ, Price) ± `UNLUPA_UI_PROGRESS.md`.

## Current Progress

### Completed Work
1. `UNLUPA_GOLDEN_RULES.md` dibuat sebagai aturan permanen.
2. Theme foundation tersedia: `ThemeProvider.tsx`, `theme.store.ts`, semantic OKLCH tokens di `src/index.css` (teal primary 0.45/0.72, off-white 0.99, navy 0.15).
3. **G0 Selesai (Unblock theme):** `LandingLayout.tsx` + `LandingPage.tsx` → `bg-background text-foreground`, `index.css` flat `bg-deep-universe`/`stars-overlay` dihapus, `tailwind.config.ts` legacy purple `#7C3AED` dihapus, `HeroSection.tsx` heading `<br />` & layout 1-kata-per-baris & kartu numpuk diperbaiki (single active slide, grid lg:grid-cols-2, `bg-card`).
4. **G1 Selesai (Landing rainbow/glass):** `PriceSection.tsx` → semua tier `bg-card border-border`, featured `border-primary bg-primary/5`, icon `bg-primary/10 text-primary`, harga `font-mono text-foreground`; `FAQSection.tsx` → `bg-card border-border`, `text-foreground`/`text-muted-foreground`, aksen `text-primary`, `font-mono`/`font-serif`; `TestimonialSection.tsx` → kartu `bg-card border shadow-sm`, avatar `bg-muted` tanpa neon; `LandingNavbar.tsx` shadow `shadow-sm` + `bg-background/80 border-border` + text `text-muted-foreground`/`text-foreground`; `LandingFooter.tsx` → `bg-card border-border`; `CtaSection.tsx` → `bg-primary/5 border-y border-primary/10` + `text-foreground` (mempertahankan copy “Ilmu Anda Berhak Terjaga.”).
5. **G2 Selesai (Auth):** `LoginLoading.tsx`/`RegisterLoading.tsx` `cosmic-loader` → `Loader2 text-primary animate-spin`, `LoginSuccess.tsx`/`RegisterSucces.tsx` `btn-submit` → `<Button>`, `ForgotPasswordForm.tsx`/`LoginForm.tsx`/`RegisterForm.tsx` raw `<input>` → `<Input>` + `<Button>`, `h-1px` → `h-px`, container glass `bg-[#0f141e]/60` → `bg-card border-border`, `text-white` → `text-foreground`. Layout editorial & business logic tidak diubah.
6. Solution/Feature/Metodologi/Inklusivitas tetap senior (akan dirapikan di gelombang berikutnya, tidak disentuh di G1 sesuai arahan).
7. **G4 Selesai (Landing senior sweep):** `SolutionSection.tsx`, `MetodologiSection.tsx`, `FeatureSection.tsx` (9 + 1 gold slide), `InklusivitasSection.tsx`, `AboutSection.tsx`, `Price/components/ShardCard.tsx` → semantik tokens. `bg-deep-universe`/`stars-overlay`/`glass-panel`/`energy-card`/`monolith-card`/`neon-icon`/glow shadows/`ring-black`/`drop-shadow-[0_0]`/`backdrop-blur-[20px]` dihapus. Cards → `bg-card border-border shadow-sm`, aksen per-slide semantik (success/info/warning/primary/destructive), gold (featured) card → `border-primary text-primary`. Metodologi divider glow + `dataFlow` animasi dihapus. ShardCard tetap pakai legacy `shard-green/blue/purple` (masih ada di index.css, hanya warna icon/label/text yang disemantikkan). `num-badge` di AboutSection dibiarkan (bukan scope G4). Grep forbidden di 6 file: **0 hits**. `npx tsc --noEmit`: **PASS**.
8. **Audit G4 (tidak diedit):** `LandingPage.tsx` clean; `HeroSection.tsx` clean; `TestimonialSection.tsx`/`FAQSection.tsx`/`CtaSection.tsx` clean.
9. **G4 selesai (Hero slider + Akad):** `AkadSection.tsx` (~64) → semantik: amber→`warning`, emerald→`success`, blue→`info`, purple→`primary`, red→`destructive`, `text-white`→`text-foreground`, `text-gray-*`→`text-muted-foreground`, `bg-white/5`→`bg-surface-1`, panel closing `bg-white/5`→`bg-card`. `HeroPagination.tsx` (1) `bg-linear-to-r from-primary to-purple-400`→`bg-primary`, `bg-white/10 hover:bg-white/30`→`bg-muted-foreground/30 hover:bg-muted-foreground/50`. `HeroNavButton.tsx` (1) `border-white/10 bg-black/40 backdrop-blur-md`→`border-border bg-card`. `Hero/slides/*` (88) → gradient-clip titles → flat `text-primary`/`text-foreground`/`text-destructive`, hapus glow shadows (`shadow-[0_0...]`, `drop-shadow-*`, `bg-clip-text`, `bg-linear-to-*`), `border-white/5`→`border-border`, `text-white/3`→`text-foreground/5`, body `text-foreground/60-90`/`text-muted-foreground`, watermark `text-white/30`→`text-foreground/30`, icon warna semantik, `bg-[#]/bg-[linear-gradient...]` bg pattern dihapus, button solid `bg-white`→`bg-primary text-primary-foreground`. `drop-shadow`/`ring-black`/`backdrop-blur-xl` bersih. `useHeroSlider.tsx` audit: tidak ada class hardcoded (murni logic scroll) — tidak perlu diedit. Grep forbidden di 11 file: **0 hits**. `npx tsc --noEmit`: **PASS**.
10. **G4 selesai (halaman pages/ + komponen shared):** `src/pages/personal/*` 6 file (ShareBooksPage, GlobalLibraryPage, PersonalPage, ModuleDetailPage, ItemDetailPage, BookDetailPage) → `bg-background`/`bg-card border-border`/`text-foreground`/`text-muted-foreground`, chips & icon ke semantic, gradient CTA `from-blue-600 to-blue-500` → `bg-primary text-primary-foreground hover:bg-primary/90`, glow shadows & `backdrop-blur-xl` dihapus, `selection:bg-blue-500/30`→`selection:bg-primary/30`, contentType accent map (blue→info, emerald→success, amber→warning, purple/indigo→primary, rose/red→destructive), `rounded-[2rem]/[2.5rem]`→`rounded-2xl/3xl`. `auth/*` 3 page shell → `bg-background text-foreground`. `AlquranPage.tsx` → `bg-background text-foreground` (font-serif dipertahankan). `NotFoundPage.tsx` → `bg-background text-foreground` + button `bg-primary text-primary-foreground` (font-display dipertahankan). `SlideContainer.tsx` (`vanishing-card`→`bg-card border-border`, watermark `text-foreground/5`). `DailyReviewSection.tsx` (components/ui) → cards `bg-card border-border`, icon/badge semantik (purple→primary, cyan→primary/info), flame `text-primary`. `DashboardPage.tsx` clean (wrapper `DashboardShell`), `ComingSoonRoomPage.tsx` sudah token. Grep forbidden: **0 hits** per file. `npx tsc --noEmit`: **PASS**.
11. **G4 selesai (Sidebar family + shared UI):** `SidebarNavItems.tsx` sisa amber/gray + indentasi rusak → `bg-primary/10 text-primary border-primary/20` / `text-muted-foreground hover:text-foreground hover:bg-surface-1` (3 NavLink). `ErrorMessage.tsx` red→`destructive` (+`-foreground`). `SuccessModal.tsx` emerald glass toast → `bg-success text-success-foreground`, icon `bg-success-foreground/15`. `HafalanCard.tsx` → `bg-card border-border hover:border-primary/50`, badge status semantik (teal→primary, amber→warning, blue→info, green→success, glow shadow dihapus), watermark `text-foreground`, hapus glow-overlay div + `backdrop-blur-sm`/`shadow-inner`. `button.tsx`/`badge.tsx` (shadcn) `text-white`→`text-destructive-foreground` pada varian destructive. `TimePurpose.tsx` `bg-red-500/10`→`bg-destructive/10`, Admin `bg-red-500 animate-pulse`→`bg-destructive`, Register/LoginSuccess `bg-emerald-500/10 text-emerald-500`→`bg-success/10 text-success`. `BookDailyReviewFlashcardModal.tsx` `border-white/10`→`border-border` (satu-satunya sisa lama — kini bersih). `EmptyState.tsx` sudah token.
12. **G4 SELESAI (verifikasi final seluruh `src/`):** Perluasan grep terakhir `text-white/|bg-white/|border-white|text-gray-|text-slate-|text-neutral-|text-red-|text-green-|text-cyan-|bg-cyan-|border-cyan-` di `src/**/*.tsx` → **0 hits**. Grep extended `text-amber-/emerald-/purple-/violet-/indigo-/rose-/blue-/teal-/sky-/orange-`, `bg-*` set, `border-*` set, `bg-deep-universe`, `bg-[#`, `shadow-[0_0`, `neon-icon`, `monolith-card`, `vanishing-card`, `glass-panel`, `glass-stat`, `ring-black`, `text-gold-premium` → **0 hits**. Sisa yang DIKENAL & sengaja (bukan pelanggaran G4): `text-transparent bg-clip-text bg-linear-to-*` dengan token semantik (gradient teks foreground/primary/warning, bukan hardcoded); custom CSS class legacy (`stars-overlay` sudah `display:none`, `num-badge`, `glass-doc`/`section-number`/`list-dot`/`list-check` di Akad, `shard-green/blue/purple` di ShardCard) → semua masuk scope **G5** (bersihkan/pertijedakan di index.css).
13. **Batch 4 temuan visual (Pra-checkpoint):** (1) `ThemeToggle.tsx` — hapus opsi System (`Monitor`) dari array & import; Light/Dark tetap, store default `system` tidak diubah. (2) `HeroSection.tsx` arrow slider — hapus `-translate-x-16/-24` yang mendorong arrow keluar viewport (terpotong `overflow-hidden` di `<main>`) saat zoom 100%; sekarang lekat `left-0`/`right-0`, visibel di 100% & 90%. (3) Slide "Daily Review Otomatis" — 3 kotak dashboard-card (`bg-primary/5`+`bg-success/5`+`bg-accent/5`) → 1 panel `bg-card border-border rounded-2xl shadow-xl` (header ikon + chip "Otomatis" pulse, baris stat 3 kolom dipisah `border-r`, strip footer FSRS/Interval; data 24/18/±32 & label dipertahankan). (4) Slide "Lupa Bukan Takdir" — grid 2 kolom sempit → panel `grid md:grid-cols-[1.15fr_1fr]` (kiri 4 fitur `bg-muted/30 border-border`, kanan "4 Tombol Feedback Kejujuran" `bg-primary/5 border-primary/10` + strip 4-petunjuk success/info/warning/primary; copy & paragraf dipertahankan persis). Semua handler/navigasi/data tidak diubah. `npm run build`: **PASS** (CSS 182.76 kB, JS 1196.88 kB). Tervalidasi dalam Tahap A.
14. **Tahap A LOLOS** — user menulis "Tahap A lolos. Lanjut Tahap B, sekalian kerjain G5." Verifikasi browser (zoom 100%/90%, Light/Dark/System, dashboard & landing) disetujui. G0–G4 + batch 4 temuan tetap BELUM di-commit.
15. **Bug hunt dashboard selesai:** 4 bug user + 1 temuan audit, semua build PASS — (1) `CreateBookModal`/`EditBookModal` submit disabled → `disabled:bg-surface-2 disabled:text-muted-foreground` (teks terbaca dua tema); (2) `DeactivateJuzModal` dirapikan jadi kartu tengah `max-w-md max-h-[90vh] overflow-y-auto rounded-2xl border-border bg-card shadow-2xl p-6 sm:p-8` + overlay `bg-black/50 backdrop-blur-sm` (hapus wrapper nested); (3) `ItemDetailActionSection` overlay hover "Ke Dashboard" `bg-surface-1` → `bg-white/15 dark:bg-black/30`; (4) `BookCard` cover image container + `overflow-hidden`; (5) `DailyReviewFlashcardModal` header tombol rating + field `headerText` (`text-*-foreground`) agar teks tak sama-warna-dengan-header. Audit harian/flashcard/modal/card/button aman (pola `bg-*`+`text-*-foreground`).
16. **G5 SELESAI (cleanup CSS legacy):** `index.css` — semua blok cosmic legacy DIHAPUS: glow-*, text-gold/premium/pain/red-fading/gold-gradient/text-glow/text-glow-white, bg-deep-universe (duplikat), vanishing-card, monolith-card (+ varian + ::before + hover), card-nebula, icon-orb, neon-icon, energy-card (+ varian + icon-glow/title-glow), icon-box, log-card, role-*, avatar-box, text-gold-gradient, particle-orbit, glass-panel, glass-stat, footer-link, faq-* (item/header/content/quote/list + accent-* + icon-chevron), cosmic-loader, btn-start, btn-submit, shard-card/shard-gold/clean-input, role-switch-btn, heatmap-*, item-row, phase-label, bg-phase-*, btn-play-*, spin-loader, shimmer-text, gate-*, num-badge, glass-doc, section-number, highlight-box, danger-box, list-check, list-dot, connector-vertical (duplikat + kedua redefinisi), stars-overlay, nav-card/nav-quran/nav-class/nav-private. **DIPERTAHANKAN:** tokens, base (`select option`), `scrollbar-hide`, `scroller`/`__inner` (marquee Testimonial) + keyframes, `thumb` (DualRange/TimeSlider), `.sidebar`/`.sidebar-overlay` (drawer token-based). Migrasi komponen: NotFoundPage (hapus div `stars-overlay` kosong), SidebarClassItems (`flex ... bg-surface-1 border hover:translate-x-1` + varian `border-{success/info/primary}/20 bg-{...}/10 hover:border-{...}/50`), ShardCard (`shard-card+shard-*` → `bg-card border rounded-2xl p-6 hover:-translate-y-1 hover:shadow-{tone}/10`; `clean-input` → Tailwind + `focus:border-[color:var(--text-color)]`, merubah `color:white` lama → `text-foreground`), HeroSection & CtaSection (`btn-submit` → solid `bg-primary text-primary-foreground rounded-xl`), AkadSection (`glass-doc`→`bg-card border-border rounded-2xl p-6 sm:p-10`, `section-number`→`font-display text-6xl text-foreground/10`, highlight/danger-box → border-l token, `list-dot/check` → `[&_li]:before:content-['•'/'✓']` tokens), AboutSection (`num-badge`→`font-display text-5xl text-foreground/10` (fix invisible-light putih/10), connector→w-px `via-foreground/10`), InklusivitasSection (connector → token). `alquran.css` `.glass-panel` (tak terpakai) dihapus. Grep seluruh `src/**` class legacy **0 hits**. `npm run build`: **PASS** (CSS 169.33 kB / gzip 21.59 kB; sebelumnya 182.76/24.72). Esklint file yang diubah: hanya sisa pre-existing (SidebarClassItems react-refresh, Hero useEffect deps; `ArrowRight` unused di Hero sudah dibersihkan).

17. **B1 SELESAI (Typography landing):** acuan `linear.md` (negative tracking display, eyebrow positif tracking, ladder line-height 1.05–1.6) & `raycast.md` (ss03 Inter). Hasil: (a) **eyebrow section landing diseragamkan** → `font-display text-xs uppercase tracking-[0.3em]` (About/Akad/Metodologi/Feature sudah Cinzel; Testimonial/Inklusivitas/FAQ yang tadinya `font-mono` ikut diharmonisasi; ukuran `text-[10px] md:text-xs` diseragamkan `text-xs`; warna aksen per section dipertahankan; Hero & slides punya sistem label mono sendiri yang konsisten — tidak disentuh); (b) **`font-cinzel` → `font-display` di seluruh landing** (Akad 11×, About 2×, Feature 1×) — perbaikan bug diam-diam: `.font-cinzel` hanya didefinisikan di `alquran.css` (diimpor hanya AlquranPage), jadi di landing font TIDAK teraplikasi (render Inter); kini benar-benar Cinzel via token `--font-display`; (c) **display heading serif diberi negative tracking** `tracking-tight` (About h1, Akad h1, Feature h2, Metodologi h2, Inklusivitas h2, FAQ h2, Price h2 — menggantikan `tracking-wide` positif di Feature/Metodologi/Inklusivitas); (d) **line-height ladder** — Solution h2 `leading-relaxed`→`leading-tight`, subhead serif `text-xl`→`leading-snug` (About 8×, Metodologi 3×, Inklusivitas 4× + 1 `leading-tight` utk `text-2xl/3xl`), Akad h3 `text-xl`→`leading-snug`; (e) **hapus class mati `text-highlight`** (AboutSpan "Prinsip" — tak ada definisi token/CSS; kini mewarisi foreground); (f) **section rhythm sudah 96px** (`py-24`) di hampir semua section — tidak diubah. Build **PASS** (CSS 169.51 kB / gzip 21.62 kB). ESLint 9 file landing: bersih. Grep `font-cinzel|text-highlight` di `src/features/landing`: **0 hits**. Catatan: `font-cinzel` tersisa di `Sidebar.tsx:35` & `ComingSoonRoomPage.tsx:53` → scope **B5**.

### In Progress
- **Tahap B (redesign visual lanjutan)** — B1 SELESAI (item 17), belum verifikasi browser. Batch berikutnya (B2 Navbar+Footer, B3 Button/Input/Badge, B4 Card & surface ladder, B5 font-cinzel non-landing) belum dipilih user.
- **Keputusan commit** — **SELESAI: commit gabungan `0d70d2d`** ("feat(ui): redesign visual tahap A (G0–G5) + bug hunt dashboard", 170 file; `tsconfig.app.tsbuildinfo` dikecualikan). Item B1 belum di-commit — menyusul setelah verifikasi browser.

### Not Finished
- **Commit item B1 (dan seluruh perubahan baru)** — menyusul setelah verifikasi browser batch (Golden Rule 5); hanya atas perintah user. `tsconfig.app.tsbuildinfo` jangan di-commit (build artifact).
- **Tahap B B2–B5** — belum dipilih user: B2 Navbar+Footer, B3 Button/Input/Badge, B4 Card & surface ladder, B5 font-cinzel non-landing (Sidebar.tsx & ComingSoonRoomPage).
- **Verifikasi browser Tahap B B1** — belum dilakukan (Light/Dark, zoom 100/90%, 1280/768/480).

## Important Files
- `UNLUPA_GOLDEN_RULES.md`
- `UNLUPA_UI_PROGRESS.md`
- `src/index.css`
- `src/components/providers/ThemeProvider.tsx`
- `src/stores/theme.store.ts`
- `src/pages/LandingPage/LandingPage.tsx`
- `src/features/landing/sections/Hero/HeroSection.tsx`
- `src/features/landing/sections/Solutions/SolutionSection.tsx`
- `src/features/landing/sections/FeatureSection/FeatureSection.tsx`
- `src/features/landing/sections/Metodologi/MetodologiSection.tsx`
- `src/features/landing/sections/Inklusivitas/InklusivitasSection.tsx`
- `src/features/landing/sections/Testimonial/TestimonialSection.tsx`
- `src/features/landing/sections/Price/PriceSection.tsx`
- `src/features/landing/sections/FAQ/FAQSection.tsx`
- `src/features/auth/`

## Current Design Direction
- Use semantic tokens, not hardcoded color per component.
- Light/Dark/System harus benar-benar terlihat berbeda dan konsisten.
- Visual style: clean, premium, focused, subtle borders, subtle elevation, intentional spacing.
- Avoid overdone glass, gradients, shadows, decorative bloat.

## Next Priority
1. **Verifikasi browser B1** — tampilkan landing (Light/Dark, zoom 100/90%, 1280/768/480): eyebrow seragam Cinzel, heading lebih rapat (tracking-tight), line-height subhead, font-cinzel kini benar-benar Cinzel. Golden Rule 5.
2. **Commit item B1** — atas perintah user; jangan include `tsconfig.app.tsbuildinfo`.
3. **Tahap B B2–B5** — user memilih batch berikutnya (Navbar+Footer / Button-Input-Badge / Card & surface ladder / font-cinzel non-landing).

## Do Not Change
- Authentication business logic, payloads, hooks, services, or API behavior.
- Backend/API assumptions.
- Al-Qur'an/FSRS/domain logic.
- Routing structure unless needed for a real bug.
- Hero Section unless bug nyata exists.

## Verification
- `npm run build`: **PASS** 2026-09-06 sesi B1 — 2091 modules, CSS **169.51 kB** (gzip 21.62 kB), JS 1199.41 kB (gzip 301.41 kB). Warn chunk-size (bukan error).
- `npx tsc -b`: PASS (no errors).
- ESLint 9 file section landing yang diedit B1: **bersih** (tanpa error).
- Grep `font-cinzel|text-highlight` di `src/features/landing`: **0 hits** (B1). `font-cinzel` tersisa: `src/components/ui/Sidebar.tsx:35`, `src/pages/dashboard/ComingSoonRoomPage.tsx:53` (scope B5).
- Grep final seluruh `src/**/*.tsx` (G4 + G5): seluruh class legacy → **0 hits**; tak berubah pada B1.
- Browser/rendered visual verification: **Tahap A LOLOS** (seluruh landing + authenticated flow). **B1 belum diverifikasi** (menyusul).
